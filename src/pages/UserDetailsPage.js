import {Outlet} from "react-router-dom";

const UserDetailsPage = () => {
    return (
        <div>
            UserDetailsPage
            <Outlet/>
        </div>
    );
};

export {UserDetailsPage};